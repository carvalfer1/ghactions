name: First Workflow
on: workflow_dispatch #trigger is waiting for a user manual intervention
jobs:
  first-job:
    runs-on: macos-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Done bye!"
